# 评测报告（快照）

> 由 `python -m agent.eval` 生成。这是**某一次运行的快照**，不是稳定结论——
> 真实端点有随机性，同一套任务多次跑结果会波动（见文末"三轮对比"）。
> 重新生成：`python -m agent.eval --json`（约 35 秒，需真实 API 凭据）。

---

## 2026-08-30 05:10 · 模型 Qwen3.5（NSCC MaaS）

    模型：Qwen3.5
    任务数：5    总耗时：34.5s

    产物验证通过：5/5（100%）
    模型自述完成：5/5（100%）

    --------------------------------------------------------------------------
    任务        考察点                       结局              步   调用      耗时    产物
    --------------------------------------------------------------------------
    calc      新建文件 + 边界条件（除 0 抛异常）  model_final     5    4    8.1s     ✓
    fixbug    读已有代码 → 定位缺陷 → 修好并验证  finish          5    5    5.2s     ✓
    csvreport 读外部数据文件 → 处理 → 输出结果    model_final     5    4    4.8s     ✓
    dedup     算法：去重但保持首次出现顺序        finish          5    5    5.7s     ✓
    lru       数据结构：LRU 缓存淘汰             model_final     5    4    6.8s     ✓
    --------------------------------------------------------------------------

    平均步数 5.0    平均工具调用 4.4    平均耗时 6.1s    平均 token 32,079
    工具失败率 0.0%（0/22）

    全部任务产物验证通过。

---

## 怎么读这份报告

两条判据并列给出，是为了让**它们的差值**暴露问题：

| | 含义 |
|---|---|
| 产物验证通过 | 把生成的产物真的跑起来、喂输入、比对输出 —— **事实** |
| 模型自述完成 | 模型是否以 `finish` / 正文收尾 —— **自述** |

两者不一致时报告会点名：

- **自述完成但验证失败 = 假完成**。最危险的一类：用户拿到跑不起来的代码还以为好了。
- **自述失败但验证通过 = 悲观失败**。能力被低估，通常是模型不敢收尾。

本次两者一致（均 5/5），没有出现偏差项。

> 本次有 3 个任务以 `model_final` 收尾（模型给出正文总结而未调用 `finish`）。
> 它们的正文是真实的完成汇报，产物也确实跑通了，因此判定成立；
> 但 `model_final` 的置信度**低于** `finish`（后者显式声明完成且通过了假完成拦截），
> 所以报告里分开列出而不合并计数。

---

## 三轮对比（同一套任务，真实端点）

| 轮次 | 产物通过 | 总耗时 | 平均步数 | 平均耗时 | 工具失败率 | 平均 token |
|---|---|---|---|---|---|---|
| ① 修复前 | 5/5 | 42.8s | 5.4 | 7.9s | 7.7%（2/26） | 35,513 |
| ② 修复后 | 5/5 | 36.5s | 5.4 | 6.7s | 0.0%（0/26） | 34,962 |
| ③ 修复后 | 5/5 | 34.5s | 5.0 | 6.1s | 0.0%（0/22） | 32,079 |

① 与 ②③ 之间修的是"空响应被当成成功收尾"（见 `docs/ITERATION-PLAN.md` 的 V10）。
②③ 的差别则纯粹是**运行间波动** —— 这就是报告里写明"单次结果不能外推"的原因。

---

## 局限（如实写）

- **样本只有 5 个**。结论只能说明"在这些任务上"，不能外推成通用能力。
- **只验证行为，不验证质量**。命名、结构、是否有多余注释都不在评分内。
- **不检查过程是否合理**。靠碰运气跑通的任务同样得满分。
- 任务只用标准库，未覆盖第三方依赖、多文件重构、跨语言等场景。
