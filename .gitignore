# ---- 凭据与本地配置（务必不入库）----
config.yaml
config.yml
config.local.yaml
config.local.json
# 密钥专用配置文件：真实凭据集中放这里，入库的只有模板 api_keys.example.yaml
api_keys.yaml
api_keys.yml
api_keys.json
# 本地一键启动脚本（内含密钥读取逻辑）
start.py
.env
.env.*
*.key
*.pem

# ---- 本地工作区数据（含个人笔记，绝不外传）----
.workbuddy/

# ---- 运行时产物 ----
.agent_backups/
.agent_sessions/
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/

# ---- Agent 默认工作区：所有生成物都落在这里，不入库 ----
workspace/

# ---- Agent 自己产生的垃圾 ----
scratch/
tmp/
.DS_Store
Thumbs.db
